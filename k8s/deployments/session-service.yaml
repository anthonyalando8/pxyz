# k8s/deployments/session-service.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: session-service
  namespace: microservices
spec:
  replicas: 3
  selector:
    matchLabels:
      app: session-service
  template:
    metadata:
      labels:
        app: session-service
    spec:
      containers:
      - name: session-service
        image: YOUR_REGISTRY/session-service:latest
        ports:
        - containerPort: 8002
        envFrom:
        - configMapRef:
            name: common-config
        env:
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: db-secret
              key: password
        - name: HTTP_ADDR
          value: ":8002"
        - name: GRPC_ADDR
          value: ":8003"
        volumeMounts:
        - name: jwt-keys
          mountPath: /app/secrets
          readOnly: true
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
      volumes:
      - name: jwt-keys
        secret:
          secretName: jwt-secret